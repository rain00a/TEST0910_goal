# PRODUCT_SPEC — 成就記錄 App（網頁版）

**最近更新：2026-09-07**
**版本：V1（新 Project，取代原生 iOS 版路線）**
**狀態：V1 開發完成（Phase 1～6 全數完成），待實際部署上線與跨瀏覽器/裝置測試**

---

## 0. Project 沿革說明

本 Project 為**新建立的獨立 Project**，前身為原生 iOS App 規劃（詳見已封存文件 `PRODUCT_SPEC_26-0903.md`）。因開發環境限制（現有 Mac 硬體無法支援 App Store 上架所需的 Xcode 版本），User 決議將產品方向改為網頁版，屬核心產品方向改變的 Major Change。

依工作法規範：**新 Project 不繼承舊 Project 的版本號與歷史**，版本從 V1 重新起算。以下功能、資料結構、UI 設計方向沿用舊 Project 已確認的產品邏輯，技術架構部分則全面改為網頁技術棧。

---

## 1. 產品定位

一款專注於「每日個人成就登記」的網頁 App。核心價值在於幫助使用者快速捕捉每天完成的事，並以**年度週次（ISO 8601）**作為主要瀏覽與組織方式，讓使用者能以「週」為單位回顧自己的累積成果。

---

## 2. 核心功能

### 2.1 新增成就
- 使用者輸入文字內容，記錄「今天做了什麼」
- 系統自動帶入登記日期（可手動調整為過去日期）
- 系統依登記日期自動換算對應的 ISO 8601 年度週次（如 `2026-W35`）
- 建立時同步寫入建立時間戳（用於同週內排序）

### 2.2 週次分組瀏覽
- 以週次為主要分組單位，橫向或直向瀏覽歷史紀錄
- 展開單一週次可看到該週逐日的成就清單

### 2.3 關鍵字搜尋
- 依成就內容關鍵字搜尋
- 搜尋結果可選擇跳轉至對應週次

### 2.4 CRUD 操作
- 新增 / 編輯 / 刪除單筆成就紀錄
- 刪除需二次確認，避免誤刪

### 2.5 Excel 匯出
- 使用者可一鍵匯出全部或指定區間資料為 `.xlsx` 檔案
- 匯出檔案包含：登記日期、年度週次、成就內容、建立時間戳

### 2.6 檢視模式切換（子畫面）
檢視模式屬於**子畫面**，用於切換不同時間範圍的瀏覽角度，與主畫面（見 2.6.1）分開：

- **週次檢視**：即第 2.2 節之週次分組瀏覽，維持膠囊時間軸 + 卡片列表呈現
- **月次檢視**：統計該月 1 日至月底的所有成就，並顯示**該月總成就數量**
- **年次檢視**：統計該年 1 月 1 日至 12 月 31 日的所有成就，並顯示**該年總成就數量**

三種模式之間可自由切換，資料來源相同，僅呈現的時間範圍與統計數字不同。

### 2.6.1 主畫面配置
主畫面聚焦於**新增物件**與**檢視當週**，並在螢幕四個角落配置延伸功能鍵：

| 位置 | 功能 |
|---|---|
| 左上角 | 檢視模式切換（週次 / 月次 / 年次） |
| 右上角 | 設定 |
| 左下角 | 搜尋 |
| 右下角 | 新增成就 |

> 網頁版備註：四角固定配置需搭配 `position: fixed` 或 sticky 定位實作，並須確認在桌機／手機瀏覽器不同視窗尺寸下，四個按鈕不與內容重疊、不被瀏覽器原生 UI（如網址列、返回手勢區）遮擋。

### 2.6.2 物件編輯
點擊任一成就物件後，會出現編輯圖示，供使用者修改：
- 成就內容（名稱）
- 登記日期

修改日期後，系統需重新計算並更新對應的年度週次。

### 2.7 成就統計（不在 V1 範圍）
> **User 決策（沿用自舊 Project，2026-09-03）：** 統計圖表屬未來設計範圍，V1 暫不深究，待後續版本視實際使用資料再行設計。

---

## 3. 資料結構

| 欄位 | 型別（網頁版對應） | 說明 |
|---|---|---|
| id | string（UUID） | 唯一識別碼 |
| registeredDate | string（ISO 8601 日期，如 `2026-09-03`） | 使用者輸入或系統帶入的登記日期 |
| weekIdentifier | string | 依 ISO 8601 計算，如 `2026-W35`，異動日期時需同步重算 |
| content | string | 使用者輸入的成就內容 |
| createdAt | number（timestamp）或 ISO 字串 | 記錄建立當下的時間，供同週排序使用 |

> monthIdentifier / yearIdentifier **不落地儲存**，一律由 registeredDate 即時計算，維持與原 iOS 版本相同的設計原則（避免衍生欄位不同步）。

---

## 4. 技術架構（網頁版，取代原生 iOS 技術棧）

| 項目 | 選擇 | 說明 |
|---|---|---|
| 前端框架 | HTML5 + CSS3 + JavaScript（或 React，視開發偏好） | 已確認的視覺原型本身即為 HTML/CSS，可直接作為實作基礎 |
| 本地儲存 | 瀏覽器端 IndexedDB | 資料儲存於使用者瀏覽器本地，無需後端伺服器 |
| 匯出格式 | .xlsx | 使用者主動觸發匯出時才轉換為 Excel 格式 |
| 匯出實作 | SheetJS（xlsx.js）函式庫 | 瀏覽器端成熟的 Excel 讀寫函式庫，前端可直接產生並下載 .xlsx 檔案 |
| 跨裝置同步 | 不採用（沿用舊決策） | 資料僅存於單一瀏覽器本地，無跨裝置同步需求 |
| 部署方式 | GitHub Pages | 已確認 |

**關鍵技術決策說明：**
資料儲存改用瀏覽器端 IndexedDB，屬於純前端（Client-side）架構，不需要後端伺服器與資料庫，可大幅降低開發與維運複雜度。此決策的風險是：資料僅存在單一瀏覽器內，清除瀏覽器快取或更換瀏覽器/裝置將導致資料遺失，需仰賴使用者養成定期 Excel 匯出備份的習慣，或在後續版本評估是否加入雲端同步。

---

## 5. UI / UX 設計方向

**風格定位：** 絢麗華麗系（Glamorous & Elaborate）— 沿用舊 Project 已確認的視覺原型

- **漸層卡片設計**：每筆成就以漸層色卡呈現，色系依週次自動輪轉（如霓虹紫 → 深藍 → 珊瑚粉）
- **玻璃擬態（Glassmorphism）**：週次總覽頁使用毛玻璃背景疊層，搭配動態模糊光暈
- **微動效**：
  - 新增成就時觸發粒子 / 煙火動畫
  - 週次卡片滑動具視差效果
  - 下拉重新整理時觸發流光動畫（網頁版可改為手動重新整理按鈕觸發，或省略此項）
- **時間軸互動**：主頁採用「時間軸 + 週次膠囊」橫向捲動設計，當週膠囊放大並發光
- **字體選擇**：標題採用具份量感的圓體或襯線體，強調紀錄的儀式感；中文字型使用 Google Fonts（如思源黑體 Noto Sans TC / 思源宋體 Noto Serif TC）
- **響應式設計**：需同時支援桌機瀏覽器與手機瀏覽器檢視，四角功能鍵、卡片列表、時間軸皆需考慮不同螢幕寬度的排版調整

> **User 決策：** 週次總覽頁視覺原型（深空背景、發光週次膠囊時間軸、毛玻璃成就卡片）已於舊 Project 確認，作為網頁版 UI 實作的視覺基準，可直接沿用對應的 HTML/CSS 原型檔案。

---

## 6. 開發階段規劃

| 階段 | 內容 |
|---|---|
| Phase 1 | 前端專案骨架建立 + IndexedDB 資料層封裝 |
| Phase 2 | 核心 CRUD 介面 |
| Phase 3 | 週次/月次/年次檢視邏輯與畫面切換 |
| Phase 4 | xlsx 匯出功能（SheetJS 整合） |
| Phase 5 | UI 視覺還原（玻璃擬態、動效）與響應式優化 |
| Phase 6 | 靜態網頁部署 |

---

## 7. 決策紀錄

**2026-09-03（沿用自舊 Project 的決策，適用於本 Project）**
- [x] 跨裝置同步：不採用
- [x] 成就統計功能：不納入 V1
- [x] 檢視模式：週次 / 月次 / 年次三種，月次與年次需顯示總成就數量
- [x] 主畫面配置：四角功能鍵（左上＝檢視模式切換、右上＝設定、左下＝搜尋、右下＝新增）
- [x] 物件編輯：點擊後透過編輯圖示修改內容與日期，日期異動連動重算週次

**2026-09-03（本 Project 新決策）**
- [x] **Major Change：產品方向由原生 iOS App 改為網頁版**。原因：開發環境（Mac 硬體）無法支援 App Store 上架所需 Xcode 版本
- [x] **新 Project 建立**：版本重置為 V1，不繼承舊 Project 版本號
- [x] **技術架構決策**：前端 HTML/CSS/JS、儲存改用瀏覽器 IndexedDB、匯出改用 SheetJS，不採用後端伺服器

**2026-09-03（開發前二次確認）**
- [x] **資料層方案二次確認**：曾評估改用 Google Apps Script (GAS) + Google Sheets 作為資料層（可換取跨裝置同步能力，且與既有專案「我的物件圖書館」的混合架構經驗一致），評估後**維持原生 IndexedDB 方案，不採用 GAS**。主要考量：V1 定位為輕量單機工具，IndexedDB 保有離線可用、低延遲、部署最簡等優勢，且跨裝置同步本非本專案既定需求；若未來確有同步需求，可另評估「IndexedDB 為主 + 手動同步備份」的折衷方案，而非直接替換資料層
- [x] **前端技術棧確認**：採用 Vanilla JS（ES Modules），不引入 React 或建置工具鏈，以維持部署簡易性；後續若功能複雜化（如加入統計圖表）可再評估是否導入框架

**2026-09-03（規格細節確認）**
- [x] **部署平台確認**：採用 GitHub Pages
- [x] **中文字型確認**：使用 Google Fonts 中文字型（思源黑體 Noto Sans TC / 思源宋體 Noto Serif TC）
- [x] **里程碑慶祝功能移除**：「連續 7 天登記」觸發全螢幕慶祝特效之功能不需要，自 V1 範圍中移除（原第 5 節「里程碑慶祝」項目已刪除）

**2026-09-07（V1 開發完成）**
- [x] Phase 1～6 全數完成：IndexedDB 資料層、CRUD 介面、週/月/年檢視切換、xlsx 匯出、關鍵字搜尋（規格 2.3，補列於 Phase 表之外）、視覺風格還原（漸層卡片/玻璃擬態/週次膠囊時間軸/新增粒子動畫）、GitHub Pages 部署文件與設定
- [x] 部署方式：純靜態網頁，無建置步驟，透過 `git push` 到 GitHub repo 的 `main` 分支 + GitHub Pages「Deploy from a branch」設定即可上線
- [x] **開發環境限制修正**：ES Modules 在 `file://` 協定下會被瀏覽器 CORS 限制擋掉，本地測試須透過本地靜態伺服器（如 `npx serve`），已寫入第 9 節環境需求與 README.md

目前無待確認事項，可進入開發階段。

---

## 8. 建議專案結構（供實作參考）

```
/src
  /db
    db.js               // IndexedDB 初始化、版本管理
    achievementRepo.js   // CRUD 封裝（add/update/delete/query）
  /utils
    isoWeek.js            // ISO 8601 週次計算 + 反算週次區間
    dateUtils.js
  /views
    weekView.js            // 週次膠囊 + 卡片列表
    monthView.js
    yearView.js
    searchView.js
  /export
    xlsxExport.js          // SheetJS 整合
  /ui
    modal.js               // 新增/編輯彈窗
    cornerButtons.js         // 四角固定按鈕邏輯
    animations.js            // 新增成就時的粒子動畫（可延後至 Phase 5；不含里程碑慶祝，該功能已移除）
  main.js                    // 進場、畫面切換狀態機
/styles
  theme.css                  // 漸層/玻璃擬態基礎變數
  layout.css
index.html
```

**資料層設計要點：**
- IndexedDB object store（如 `achievements`），主鍵 `id`（UUID）
- 建兩個 index：`weekIdentifier`（週次檢視查詢用）、`registeredDate`（月/年統計與排序用）
- 月次／年次統計數字不落地儲存，一律以 `registeredDate` 做 range query 即時計算，避免衍生資料與來源資料不同步
- `isoWeek.js` 為全系統核心依賴（週次分組、月/年統計、資料結構 weekIdentifier 皆賴其正確性），建議 Phase 1 即完成並涵蓋跨年邊界（12/29～1/4）測試案例

---

## 9. 環境與能力（供接手開發者/AI 參考）

### Required（缺少會無法完成）
- 一般文字編輯器或 IDE（如 VS Code）
- 現代瀏覽器（Chrome / Safari / Edge 等，支援 IndexedDB API）
- **本地靜態伺服器工具**（如 `npx serve`、`python3 -m http.server`）：本專案採用 ES Modules，
  瀏覽器會擋掉透過 `file://` 協定載入的模組（CORS 限制），開發測試時不能直接雙擊開啟
  `index.html`，須先啟動本地伺服器再瀏覽

### Optional（缺少仍可工作）
- Node.js + npm（若採用 React 或建置工具鏈；純 HTML/CSS/JS 方案不需要）
- GitHub 帳號（GitHub Pages 部署用，僅部署階段需要）

### AI Capability
- 無特殊 AI 能力需求，一般前端網頁開發能力即可完成

### External Service
- SheetJS（xlsx.js）：開源函式庫，透過 CDN 或 npm 引入，無需申請帳號或 API Key

---

## 10. 測試環境矩陣

### 瀏覽器（必測）
IndexedDB、SheetJS 匯出、CSS 玻璃擬態/動效皆為瀏覽器端功能，至少須覆蓋：
- Chrome（桌機 + Android）
- Safari（桌機 macOS + iPhone）— IndexedDB 實作與手機手勢區遮擋問題需重點驗證
- Edge（桌機）

### 裝置／螢幕尺寸
對應 2.6.1 節四角固定按鈕在不同視窗尺寸下不可被遮擋的要求：
- 桌機：寬螢幕（約 1920px 寬）、較窄筆電螢幕（約 1366px 寬）各一
- 手機：至少一支 iPhone（含瀏海/手勢區，驗證 `safe-area-inset`）、一支 Android
- 平板尺寸（可選）

### 作業系統
- macOS（開發機本身）
- Windows（驗證非 Mac 環境下 Chrome/Edge 顯示正常）
- iOS / Android（行動端測試）

### IndexedDB 特殊測試情境
對應第 4 節已知風險（清除瀏覽器快取導致資料遺失）：
- 清除瀏覽器快取/資料後，確認 App 能正常重新初始化（而非壞掉）
- 瀏覽器無痕模式下的行為（部分瀏覽器對 IndexedDB 有配額或行為限制）
- 儲存空間接近配額上限時的行為（非必要，可記錄為已知風險）

### xlsx 匯出測試
- 匯出後以 Excel / Numbers / Google Sheets 開啟，確認中文內容編碼正常

---

*本文件為本 Project 的 Product Source of Truth。若程式碼、其他文件與本文件產生衝突，以本文件為最高權威。本文件可脫離原始對話獨立成立，供後續 AI 或開發者直接接手使用。*
